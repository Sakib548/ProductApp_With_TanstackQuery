{
  "name": "server",
  "version": "1.0.0",
  "description": " Mock API Server created with json-server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch ./database/data.json"
  },
  "author": "Sakib",
  "license": "ISC",
  "dependencies": {
    "json-server": "^1.0.0-beta.0"
  }
}
